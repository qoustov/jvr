# Jennifer Victoria

Portfolio website for Jennifer Victoria, voice actor, singer and performer.

## Local development

```sh
npm start
```

Open `http://localhost:3000`. Health check: `GET /api/health`.
